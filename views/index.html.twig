<!DOCTYPE html>
<html>
    <body>
        <div class="container">
            <h1>Epic Shoe Directory</h1>
            <img class="homeButton" src="/img/shoes.jpeg" alt="An image of some shoes" />
            <div class="row">
                <div class="col-sm-6">
                    <form class="form-inline" action="/add_brand" method="post">
                        <div class="form-group">
                            <input class="form-control" type="text" name="new_brand" placeholder="Add a new brand" required>
                        </div>
                        <button type="submit" class="btn btn-success">+</button>
                    </form>
                    <h3>Brands:</h3>
                    {% if brands %}
                        {% for brand in brands %}
                        <h4><a href="/brand/{{ brand.getId }}">{{ brand.getName }}</a></h4>
                        {% endfor %}
                        <form class="" action="/delete_brands" method="post">
                            <button type="submit" class="btn btn-danger">Delete All</button>
                        </form>
                    {% endif %}
                </div>
                <div class="col-sm-6">
                    <form class="form-inline" action="/add_store" method="post">
                        <div class="form-group">
                            <input class="form-control" type="text" name="new_store" placeholder="Add a new store" required>
                        </div>
                        <button type="submit" class="btn btn-success">+</button>
                    </form>
                    <h3>Stores:</h3>
                    {% if stores %}
                        {% for store in stores %}
                            <h4><a href="/store/{{ store.getId }}">{{ store.getName }}</a></h4>
                        {% endfor %}
                        <form class="" action="/delete_stores" method="post">
                            <button type="submit" class="btn btn-danger">Delete All</button>
                        </form>
                    {% endif %}
                </div>
            </div>
        </div>
    </body>
</html>
